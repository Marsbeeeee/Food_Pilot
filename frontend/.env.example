# 后端 API 地址（前端发请求时用，默认 http://localhost:8000）
# 注意：3000 是前端端口，8000 是后端端口，这里必须填后端
VITE_API_BASE_URL=http://localhost:8000

# AI Key（建议放在项目根目录 .env，后端会读取 frontend/.env.local）
# Gemini: GEMINI_API_KEY=xxx
# 千问: DASHSCOPE_API_KEY=sk-xxx 且 AI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
