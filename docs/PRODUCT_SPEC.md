# Food Pilot 产品规格说明

## 文档信息

- 文档状态：已按当前仓库实现重整
- 更新时间：2026-03-31
- 事实来源：以后端路由、服务层、前端页面、数据库初始化脚本、测试基线为准
- 适用范围：`backend/`、`frontend/`、`docs/` 当前实现

---

## 1. 产品定位

Food Pilot 是一个以对话为主入口的营养助手。当前版本的核心价值不是“自动记全量饮食流水”，而是：

1. 用户通过 Chat 提问，获得餐食推荐或营养估算。
2. 用户将有价值的结果显式保存到 Food Log。
3. 用户基于保存结果进入 Insights 做日/周分析。
4. Profile 为推荐、估算和安全约束提供个性化上下文。

当前产品更接近：

`对话式营养助手 + 显式保存的结果库 + 基础饮食分析面板`

而不是：

- 自动饮食日记
- 医疗诊断工具
- 完整的多模态拍照识别产品

---

## 2. 当前信息架构

前端当前实际由四个一级视图组成：

- `WORKSPACE`：Chat 主工作区
- `EXPLORER`：Food Log 列表与详情
- `INSIGHTS`：复用 Explorer 内的分析视图
- `PROFILE`：个人档案

主链路为：

`注册/登录 -> Chat 提问 -> 获得推荐/估算 -> 显式保存到 Food Log -> 加入 Insights 分析篮子 -> 生成日/周分析 -> 回到 Chat 或 Profile 继续调整`

---

## 3. 当前版本模块状态

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| Auth | 已实现 | 注册、登录、会话恢复、删除当前账号均已接后端 |
| Chat / Assistant | 已实现 | 会话管理、消息发送、规则分流、推荐/估算/澄清已落地 |
| Food Log | 已实现 | 保存、查询、详情、编辑、软删除、恢复、来源回跳已落地 |
| Profile | 已实现 | 单用户单档案、热量目标计算、过敏与饮食风格已落地 |
| Insights | 已实现核心闭环 | 分析、历史快照、分析篮子后端同步已落地 |
| 独立 `/estimate` API | 已实现（系统能力层） | 作为结构化估算能力保留，不作为当前版本主入口 |
| 中文食物知识库 | 已实现首期版本 | 已有离线种子库、检索与引用透传；下一阶段聚焦标准化扩容与更贴近中国用户的真实数据覆盖 |
| 测试基线 | 已定义 | 发布门禁为后端测试 + 前端单测，E2E 作为条件性集成验证 |

---

## 4. 功能规格

### 4.1 Auth

后端接口：

- `POST /auth/register`
- `POST /auth/login`
- `GET /auth/me`
- `DELETE /auth/me`

当前约束：

- 注册需要合法邮箱、非空 `displayName`、至少 8 位密码。
- 使用 Bearer Token。
- Token 为后端自签 HMAC JWT，包含 `sub / iat / exp`。
- 前端会将 token 与当前用户信息保存在本地。
- 删除账号后，数据库通过外键级联删除该用户相关 Profile、Chat、Food Log、Insights 数据。

### 4.2 Chat / Assistant

#### 4.2.1 会话能力

后端接口：

- `POST /chat/sessions`
- `GET /chat/sessions`
- `GET /chat/sessions/{session_id}`
- `PATCH /chat/sessions/{session_id}`
- `DELETE /chat/sessions/{session_id}`
- `POST /chat/messages`
- `POST /chat/sessions/{session_id}/messages`

当前行为：

- 支持新建空会话。
- 支持在空会话中直接发首条消息并自动生成会话。
- 首条用户消息会用于更新默认标题。
- 删除会话为永久删除，不做软删除。
- 删除会话后，Food Log 记录仍保留，但关联的 `session_id` 会被清空，前端展示为 `Source Chat Deleted`。

#### 4.2.2 消息类型契约

对外消息类型统一为：

- `text`
- `meal_estimate`
- `meal_recommendation`

兼容规则：

- 数据库内部仍可能存储历史值 `estimate_result`
- Router / Schema 层统一映射为 `meal_estimate`

#### 4.2.3 意图分流

当前实现是“规则分流”，不是 LLM 分类器。

分流结果：

- `meal_estimate`：命中“多少热量 / 多少蛋白质 / 营养怎么样 / 大概多少”等估算关键词
- `meal_recommendation`：命中“推荐 / 替代 / 选哪个 / 训练后吃什么 / 怎么优化”等推荐关键词
- `text`：命中解释、说明、区别、为什么等文本类关键词
- `_clarification`：输入模糊、像问题但又不像具体食物描述时，先返回澄清提问

补充说明：

- 估算优先级高于推荐。
- 解释型追问会优先走 `text`，避免把“为什么推荐这个”误判成新的推荐请求。
- 当用户输入触发了估算意图，但菜品标准化匹配置信度不足时，Chat 应优先进入 `_clarification` 追问，而不是直接生成结构化估算结果。
- 典型场景包括“炒面热量”“盖饭热量”“套餐多少卡”这类标准菜品指向不清、缺少关键修饰词或可能对应多个标准菜的表达。
- 当前规则对高频中文问法已有回归测试覆盖，但复杂表述仍存在误分流风险。

#### 4.2.4 Assistant 输出边界

- Chat 内估算结果不会自动写入 Food Log。
- Chat 内推荐结果不能直接保存到 Food Log。
- 多食物估算时，消息 `payload.estimates[]` 会保留每个子估算块，前端可逐块展示。
- Chat 估算结果采用“两层组织逻辑”：
  1. 最外层先判断用户是否描述了多个不同菜品或独立食物。
  2. 若是，则按“一个菜品 / 独立食物一张卡”拆开展示；若否，则只展示一张卡。
- 在每一张菜品卡片内部，都应展示当前菜品的组成项明细、各组成项对应的热量与三大营养素，以及该菜品总热量。
- Chat 估算结果的“拆卡”粒度是“菜品 / 独立食物”，不是单个配料或烹调用料。
- 当用户在同一句里明确提到多个菜品或独立食物时，例如“番茄炒蛋和肉末茄子”或“辣椒炒肉和白米饭”，应拆成多个估算卡片。
- 当用户问的是一个单独菜品时，例如“肉末茄子热量多少”，即使系统需要展示茄子、肉末、植物油等组成项，也必须保留为一个菜品卡片，并在该卡片内部按食材分行展示。
- 单菜品内部的食材拆解属于卡片内明细，不得提升为 `payload.estimates[]` 的并列卡片。
- 对单菜品估算，优先输出“菜名 + 组成项表格”的结构，效果应接近“番茄炒蛋盖饭 -> 白米饭 / 鸡蛋 / 番茄 / 烹饪用油”这种单卡内逐行拆解。
- 对多菜品估算，`payload.estimates[]` 中的每个 estimate block 都应对应一个单独菜品卡片；每个 block 自身的 `items[]` 应优先是该菜品的组成项表格，而不是只有一条聚合总项。
- 若标准菜品匹配仍处于低置信状态，Chat 不应直接输出可保存的信息卡片，而应先追问用户补足关键信息，如做法、主配料、甜度、品牌、是否带浇头等。

#### 4.2.5 推荐安全约束

当前实现：

- 当请求携带 `profileId` 时，推荐链路会读取 Profile。
- 如果 Profile 中存在 `allergies`，系统会对推荐结果做关键词级过敏原拦截。
- 命中过敏原时，不返回原推荐，而返回“推荐已拦截”的安全提示。

当前边界：

- 这是关键词级拦截，不是结构化配料理解。

#### 4.2.6 Chat 估算卡片层级强约束（防回归）

适用范围：

- Chat 中所有 `meal_estimate` 消息的结构组织与前端渲染。
- 包括 `payload.items[]` 单卡模式与 `payload.estimates[]` 多卡模式。

目标：

- 统一为“菜品 / 独立食物是最外层卡片；组成食材与营养素在卡片内部展示”。
- 防止再次出现“把一个菜拆成多个外层卡片”的错误。
- 在不改变当前估算卡片 UI 形态（卡片样式、表格结构、保存区布局）的前提下，保证“多菜品输入 = 多张并列外层卡片”。

术语定义：

- `外层卡片（dish card）`：用户一次描述中的一个“直接食用对象”（菜品或独立食物/饮品）。
- `内部组成项（ingredient row）`：该外层卡片内部的食材/配料行，按行展示份量与营养素。
- `顶层拆卡`：`payload.estimates[]` 的并列 block 数量。

强约束（MUST）：

1. 顶层拆卡单位必须是“菜品 / 独立食物”，不能是食材。
2. 单菜品输入必须只生成 `1` 张外层卡片。
3. 单菜品内部的拆解（如猪肉、青椒、植物油）必须仅出现在该卡片 `items[]` 中。
4. 多菜品输入时，`payload.estimates[]` 每个 block 对应一个菜品或独立食物；不得把同一菜品内部食材提升为并列 block。
5. 对“牛肉汉堡”这类复合菜名，默认仍是 `1` 张“牛肉汉堡”外层卡；其面包、肉饼、酱料是内部组成项，不得成为并列外层卡。
6. 只有当用户明确把组成部分作为“分别吃的独立对象”表达时，才允许拆成多个外层卡（例如“我吃了牛肉饼和白面包”）。
7. 若当前无法稳定判断用户说的是一个标准菜还是多个独立对象，必须走澄清提问，不得直接输出错误层级卡片。
8. 当用户一次输入多个菜品/饮品时，前端必须按既有卡片 UI 重复渲染多个同结构卡片（每个菜品一张），禁止把多个菜品合并为一张“组合总卡”。

禁止模式（MUST NOT）：

- 用户问“牛肉汉堡热量”时，输出两个并列外层卡：“瘦牛肉饼” + “汉堡面包（白面包）”。
- 用户问“辣椒炒肉热量”时，把“辣椒”“猪肉”“油”作为三张并列外层卡。
- 用食材名替代菜名作为单菜品外层卡标题（除非用户明确只问该食材本身）。
- 用户问“今天中午吃了牛肉汉堡和一杯柚子苏打水”时，仅输出一张“牛肉汉堡配柚子苏打水”合并卡。

数据契约要求（Chat payload）：

- 单菜品模式：
  - `payload.estimates[]` 可为空或不存在。
  - 外层卡使用 `payload.title + payload.items[]` 渲染。
  - 若存在 `itemization_mode = single_dish_ingredients`，前端必须强制按“单外层卡 + 内部组成项”渲染，不得再拆卡。
- 多菜品模式：
  - `payload.estimates[]` 必须存在且每个 block 是一个菜品/独立食物。
  - 每个 block 的 `items[]` 表示该菜品内部组成项，而非新的顶层拆卡候选。

标准示例（应当通过）：

- 输入：“辣椒炒肉热量多少”
  - 外层卡片数：`1`
  - 外层卡片标题：`辣椒炒肉`
  - 内部组成项：`猪瘦肉/青椒/植物油...`
- 输入：“今天中午吃了牛肉汉堡和一杯苏打水”
  - 外层卡片数：`2`
  - 外层卡片标题：`牛肉汉堡`、`苏打水`
  - `牛肉汉堡` 卡内可有：`汉堡面包/牛肉饼/酱料...`
- 输入：“今天中午吃了牛肉汉堡和一杯柚子苏打水”
  - 外层卡片数：`2`
  - 外层卡片标题：`牛肉汉堡`、`柚子苏打水`
  - 不允许标题聚合为 `牛肉汉堡配柚子苏打水`
- 输入：“我吃了牛肉饼和白面包”
  - 外层卡片数：`2`
  - 外层卡片标题：`牛肉饼`、`白面包`

回归验收清单（每次改动 Chat 估算展示逻辑时必测）：

1. 单菜品必须保持单外层卡。
2. 单菜品内部组成项必须保留在卡内表格，不得升为并列外层卡。
3. “复合菜名（如汉堡/盖饭/炒饭）”不得被错误拆为其内部配料外层卡。
4. 多菜品输入必须一菜一卡，且每卡内部可继续展示组成项。
5. 低置信场景优先澄清，不输出错误层级的可保存结果卡。
6. 在多菜品场景下，保持现有单卡 UI 样式不变，仅增加卡片数量，不新增“组合汇总卡”替代逐菜卡。

### 4.3 独立 Estimate API（系统能力层）

后端接口：

- `POST /estimate`

#### 4.3.1 产品定位

`/estimate` 是系统级的结构化营养估算能力接口，不作为当前版本的用户主入口。

其主要职责：

- 为 Chat 提供底层估算能力
- 为 Food Log 提供结构化数据来源
- 为未来快速录入、批量估算和外部接入提供能力基础

#### 4.3.2 与 Chat 的关系

- Chat 是用户主入口，负责意图理解、澄清和结果组织。
- `/estimate` 是原子能力层，仅负责单次估算计算。
- Chat 在命中 `meal_estimate` 时，应通过统一估算链路调用 `/estimate` 对应能力，而不是发展独立估算逻辑分支。
- 当菜品标准化匹配置信度不足时，Chat 先负责澄清追问；只有在补足信息、达到可稳定映射标准菜品的条件后，才继续进入结构化估算与后续保存链路。

#### 4.3.3 当前入口策略

- 当前版本不提供独立 Estimate 页面或主按钮入口。
- 所有用户估算行为优先通过 Chat 触发。
- `/estimate` 仅作为内部能力和前端 client 保留。

#### 4.3.4 后续演进方向（非当前版本）

未来可能扩展为：

- Quick Add（无对话快速录入）
- 批量食物估算入口
- 第三方 API / 多端复用能力
- 面向中国用户的标准化食物数据底座

其中“面向中国用户的标准化食物数据底座”下一阶段的产品要求为：

- 继续扩充中国高频菜品、早餐、外卖套餐、饮品、便利店与健身餐覆盖。
- 为知识条目建立稳定 `food_id`、标准名、中文别名、份量提示、营养值来源与更新时间。
- 优先提升“中文真实菜品匹配与估算稳定性”，而不是先追求通用国际食物大全。
- 数据形态允许继续以离线版本化数据集为主；是否迁移为独立表或独立数据库，取决于后续后台运营、批量维护与多来源同步需求。

#### 4.3.5 不做事项（当前阶段）

- 不作为 Chat 的替代入口
- 不提供与 Chat 重叠的推荐或解释能力
- 不在 UI 中形成与 Chat 并列的主功能模块

#### 4.3.6 请求与返回结构

请求支持：

- `query`
- `clientRequestId`
- `profileId`
- `sessionId`

返回结构包含：

- `success`
- `data`
- `error`
- `clientRequestId`
- `foodLogId`
- `saveStatus`

补充约束：

- 多菜品场景下，`data.items[]` 的语义是“顶层菜品 / 独立食物项”，Chat 会进一步拆成多个卡片。
- Chat 在将多菜品结果拆成多个卡片后，应继续逐卡尝试补齐该菜品的组成项明细，使每张卡都能展示“组成项行 + 该菜品总量”。
- 单菜品场景下，`data.items[]` 可以直接承载该菜品的组成项明细；此时结果必须保留为一个卡片，并通过 `itemization_mode = single_dish_ingredients` 约束前端不要再拆卡。
- 若命中的是单个标准菜品，系统应优先输出可直接渲染为单卡表格的组成项明细，而不是只返回一条总项。

### 4.4 Food Log

#### 4.4.1 产品定位

Food Log 是“显式保存的结果库”，不是自动记录所有饮食行为的流水账。

#### 4.4.2 支持来源

当前支持三类来源：

- `chat_message`
- `estimate_api`
- `manual`

#### 4.4.3 保存规则

- 只有用户主动保存时，才创建 Food Log。
- `chat_message` 来源必须带 `session_id`。
- `chat_message` 只能保存结构化估算结果。
- 文本回复与推荐回复不能直接保存到 Food Log。
- `/estimate` 成功响应也不会自动保存，仍需显式调用保存接口。

#### 4.4.4 幂等与去重

- Food Log 支持 `idempotencyKey`。
- 数据库对 `(user_id, idempotency_key)` 建唯一索引。
- Chat 保存默认使用消息相关 key。
- 独立 estimate 保存默认使用 `estimate_api:{clientRequestId}`。
- 如果命中已删除记录，系统会先恢复再更新，而不是新建重复条目。

#### 4.4.5 列表与检索

后端接口：

- `GET /food-logs`
- `GET /food-logs/{food_log_id}`
- `POST /food-logs`
- `POST /food-logs/from-estimate`
- `PATCH /food-logs/{food_log_id}`
- `DELETE /food-logs/{food_log_id}`
- `POST /food-logs/{food_log_id}/restore`

当前列表过滤能力：

- `sessionId`
- `dateFrom`
- `dateTo`
- `query`
- `sort`
- `limit`

当前排序能力：

- `created_desc`
- `created_asc`

当前状态判断：

- “优化 Food Log 的检索与筛选能力”最轻量级部分已完成。
- 现阶段已从仅按 meal 过滤，升级为支持关键词查询与排序。
- 更复杂的多维筛选仍未进入当前实现。

#### 4.4.6 编辑与生命周期

- 删除为软删除。
- 支持恢复已删除条目。
- 前端允许编辑食材克重，并按比例重算热量和三大营养素展示。
- 列表默认只返回 `active` 条目。

#### 4.4.7 图片与元数据

Food Log 当前已支持：

- `image`
- `imageSource`
- `imageLicense`

当前行为：

- 若有图片但未传来源，后端会给默认来源值。
- 若有图片但未传授权，后端默认 `user_owned`。
- 前端无图时展示占位图。
- 详情页展示来源与授权信息。

#### 4.4.8 标准菜品图片自动补齐（下一阶段需求）

产品目标：

- 为命中标准菜品的 Food Log 自动补齐统一、稳定的官方菜品图。
- 提升 Food Log 列表识别效率与整体产品质感。
- 建设可持续维护的“标准菜品视觉资产库”，而不是把图片能力绑定在单条 Food Log 上。

核心策略：

- 图片生成、存储与审核的粒度是 `standard_dish`，不是 `food_log`。
- 每个标准菜品只维护一张官方默认图；同一道标准菜品命中的所有 Food Log 复用同一张图。
- AI 生成只在标准菜品尚无图片时触发一次；审核通过后全局复用，除非人工重置，否则不重复生成。
- AI 生成结果不直接面向用户展示，必须先进入后台审核队列。

触发与复用逻辑：

- 当 Food Log 保存过程中，高置信命中标准菜品时，系统为该记录绑定 `standard_dish_id`。
- 若 `standard_dish.image_url` 为空，则异步触发 `image_generation_job`。
- 若 `standard_dish.image_status = approved`，则 Food Log 可直接复用该标准菜品图片。
- 若未命中标准菜品、命中置信度不足，或图片仍处于 `pending` / `null` 状态，则不阻塞保存，前端继续展示占位图。
- 若用户当前提问本身只得到低置信标准菜品候选，Chat 层应优先发起澄清追问，避免直接产出模糊菜品卡片并把不稳定条目写入 Food Log 或标准菜品资产链路。

统一生成规范：

- 所有标准菜品图必须使用统一的 Prompt Template 生成，禁止不同菜品使用分散、风格不一致的临时 prompt。
- 用户侧输入应保持极简：普通用户或上游业务层只需要提供菜名 / 标准菜品名，不应要求用户手写详细出图 prompt。
- 系统必须在图片生成链路内部完成 Prompt 扩写：基于标准菜品名自动补齐构图、容器、视角、摆盘、背景与光线等描述，再与统一风格模板拼装为最终 prompt。
- Prompt 扩写应优先采用可控、可回归的规则模板或受控标签映射，而不是把是否能生成好图完全交给用户输入质量。
- 当系统能识别菜型时，应按菜型补齐细节；例如汤面类补齐“碗装 / 可见汤面 / 热气感”，盖饭类补齐“米饭打底 / 主菜覆盖”，披萨类补齐“圆形薄饼 / 融化芝士”，甜品类补齐“精致摆盘 / 柔和甜品光线”。
- 当菜型识别不足或无法稳定分类时，系统应回退到通用美食摄影扩写模板，而不是直接使用过短的裸菜名 prompt。
- 当前推荐模板要求包括：`菜品主体单一且居中`、`约 45 度轻微俯视角`、`米白/浅灰干净背景`、`柔和自然光`、`真实但略理想化`、`无人物/手/明显餐具干扰`、`无文字/水印/logo`、`适合作为产品卡片封面`。
- 统一 prompt 的目标是保证视觉资产库风格稳定，避免同类菜品出现“照片感、插画感、背景复杂度”明显不一致的问题。

审核与入库流程：

1. AI 根据统一模板生成标准菜品候选图。
2. 候选图进入后台审核队列，并写入候选记录或将标准菜品标记为 `pending`。
3. Admin 页面展示待审核列表，支持查看、通过、拒绝与重新生成。
4. 审核通过后，将图片标记为该标准菜品的官方图，并写回 `standard_dish.image_url` 与 `standard_dish.image_status = approved`。
5. 审核拒绝后，候选图保留审计状态或被丢弃，并允许重新发起生成。

数据结构建议：

- `standard_dishes`
  - `id`
  - `name`
  - `image_url`
  - `image_status` (`null / pending / approved`)
- `dish_images`（可选扩展表）
  - `id`
  - `dish_id`
  - `image_url`
  - `source = ai_generated`
  - `status = pending / approved / rejected`
  - `created_at`

治理要求：

- 标准菜品官方图属于受控产品资产，应继续保留来源与授权审计字段，沿用现有 `image / imageSource / imageLicense` 治理思路。
- Admin 审核通过前，候选图不得直接出现在用户侧 Food Log 列表或详情中。
- Admin 审核页面属于内部运营工具，只对管理员开放，不进入普通用户的信息架构、主导航或常规设置入口。
- 图片补齐是展示增强层，不改变估算结果，不参与识别主链路，也不影响 Food Log 主保存成功与否。

当前阶段不做事项：

- 不做开放式网络搜图或抓图后直接入库。
- 不允许用户上传图片参与标准菜品图建设。
- 不把图片作为菜品识别入口。
- 不承诺所有自由文本菜名都能自动补齐图片。

### 4.5 Profile

后端接口：

- `POST /profile`
- `GET /profile/me`
- `GET /profile/{profile_id}`
- `PUT /profile/{profile_id}`

当前字段：

- `age`
- `height`
- `weight`
- `sex`
- `activityLevel`
- `exerciseType`
- `goal`
- `pace`
- `kcalTarget`
- `dietStyle`
- `allergies`

当前业务规则：

- 一个用户只允许一个 Profile。
- 前端有完整 Profile 页面。
- 前端会根据年龄、身高、体重、性别、活动水平、目标、节奏自动计算推荐热量并回填 `kcalTarget`。
- 前端会本地缓存当前 Profile，并把 `profileId` 传入 Chat / Estimate。

Profile 对其他模块的影响：

- 推荐链路使用目标、热量目标、饮食风格、过敏原和活动信息。
- 估算链路会把 Profile 作为建议文案上下文。
- 过敏原会参与推荐安全拦截。

### 4.6 Insights

后端接口：

- `POST /api/insights/analyze`
- `GET /api/insights/history`
- `GET /api/insights/basket`
- `PUT /api/insights/basket`

#### 4.6.1 当前模型

Insights 当前应采用“左侧实时聚合 + 右侧 AI 快照”的双轨模型，而不是整页统一沿用历史快照。

核心原则：

- 左侧基础数据区与右侧 AI 分析区是两个独立状态源，不能绑定为同一次刷新动作。
- 左侧基础数据区展示的是“当前已选菜品”的实时聚合结果，不依赖用户点击“生成 AI 分析 / 重新分析”。
- 右侧 AI 分析区展示的是显式生成的 snapshot，只有在用户主动触发分析后才更新。
- Food Log、分析篮子或当前日期范围内的已选菜品发生变化时，左侧应立即同步；右侧继续保留上一次 AI 快照，直到用户重新分析。
- 历史结果优先只适用于右侧 AI 分析区，不适用于左侧基础数据区。

#### 4.6.2 分析维度

当前支持：

- `day`
- `week`

分析输入：

- `mode`
- `dateRange`
- `selectedLogIds`（可选）
- `cacheKey`（前端可传）

分析输出：

- `aggregation`：总热量、蛋白、碳水、脂肪、比例、条目数
- `entries`：参与分析的简要条目
- `ai`：`summary / risks / actions`

补充产品语义：

- `aggregation` 属于可由当前已选菜品直接计算出的基础数据，可实时刷新。
- `ai` 属于解释性产物，必须通过显式分析动作生成，不做隐式重跑。

#### 4.6.3 历史快照与唯一性

持久化层当前规则：

- `insights_analysis` 以 `user_id + mode + date_start + date_end` 唯一。
- 同一时间范围下重新分析，会覆盖旧结果。
- `selectedLogIds` 会被保存，但不会形成同一时间范围下的多版本并存。

这意味着：

- 当前版本的产品语义是“每个时间范围保留最新分析快照”。
- 它不是“同一天/同一周可保存多套筛选版本”的历史版本系统。

#### 4.6.4 左右分区职责与更新规则

左侧基础数据区包括但不限于：

- 总热量
- 宏量营养素分布
- 已选菜品列表
- 基于当前已选菜品可直接得出的计数、进度和汇总指标

右侧 AI 分析区包括但不限于：

- AI 饮食建议
- 综合评估
- 风险提示
- 行动建议
- “生成 AI 分析 / 重新分析”触发入口

更新规则：

1. 只要当前日期范围内有菜品被添加、删除、替换、编辑，或分析篮子选择发生变化，左侧基础数据区就应立即更新。
2. 左侧更新不应要求用户点击“重新分析”，也不应等待历史快照返回。
3. 右侧 AI 分析区继续维持显式触发语义，仅在用户点击“生成 AI 分析 / 重新分析”后更新。
4. 当左侧当前数据与右侧 AI 快照的输入不一致时，应明确提示“右侧 AI 结果可能已过期/基于历史快照”，但不能阻塞左侧刷新。
5. 左侧和右侧可以同时展示不同时间点的数据语义：左侧是当前选择，右侧是最近一次分析快照；这种差异必须被产品文案清楚表达。

#### 4.6.5 页面加载逻辑

当前前端行为：

1. 先读取当前日期范围内的已选菜品/分析篮子，用于计算左侧基础数据区
2. 左侧基础数据区优先展示当前实时聚合结果
3. 同时读取 `GET /api/insights/history`，用于恢复右侧最近一次 AI 快照
4. 若当前时间范围已有历史结果，则右侧展示该快照
5. 若没有历史结果，则右侧展示空状态并提供“生成 AI 分析”入口
6. 分析成功后，仅更新右侧 AI 结果快照，并将当次输入保存为该时间范围的最新快照
7. 后续若菜品继续变化，左侧继续实时更新；右侧保持最近一次分析结果，并显示时效提醒

#### 4.6.6 分析篮子同步

当前实现已支持分析篮子后端同步：

- 前端本地仍保留 basket 状态，保证交互即时性
- 同时通过 `/api/insights/basket` 做账号级同步
- 换设备或重新登录后，可恢复已同步的 basket 项

当前边界：

- basket 是“待分析条目选择状态”，不是正式分析历史
- 它没有版本比较或多人协作语义

补充要求：

- basket 变化应首先驱动左侧基础数据区刷新，因为它直接改变“当前已选菜品”的集合。
- basket 不应自动触发新的 AI 分析任务；是否更新右侧 AI 文本，仍由用户显式决定。

#### 4.6.7 周分析展示状态

当前周分析已经具备日/周双模式、历史快照和 AI 文本输出能力，但仍不是完整的趋势分析产品。

现阶段更接近：

- 基础周视图
- 基础聚合结果
- AI 周期总结

尚未形成更强的趋势洞察能力，例如：

- 更长周期对比
- 多版本对比
- 明确的过期标记
- 结构化趋势结论

---

## 5. 技术与工程现状

### 5.1 技术栈

- 前端：React 19 + TypeScript + Vite
- 后端：FastAPI + SQLite
- AI 调用：后端统一发起
- 认证：Bearer Token + HMAC JWT

### 5.2 AI 配置现状

当前后端支持两类配置：

- Gemini 风格配置
- DashScope / Qwen 的 OpenAI 兼容配置

本仓库文档和 `.env.example` 的主推荐路径更偏向：

- `DASHSCOPE_API_KEY`
- `AI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1`
- `AI_MODEL=qwen-plus`

### 5.3 数据库约束

当前数据库关键约束：

- `users.email` 唯一
- `profiles.user_id` 唯一，保证单用户单档案
- `food_logs(user_id, idempotency_key)` 唯一
- `insights_analysis(user_id, mode, date_start, date_end)` 唯一
- `chat_sessions / messages / food_logs / insights_*` 都与 `users` 存在明确外键关系

补充说明：

- 当前用户业务数据持久化以 SQLite 为主。
- 中文食物知识当前以仓库内版本化离线数据集提供，不等同于“完整运营型食物主数据库”。
- 下一阶段如引入更大规模中国食物标准库、后台运营维护、标准菜品视觉资产绑定关系与多来源同步，再评估是否拆分为独立表结构或独立数据库。

### 5.4 测试基线

当前 `docs/TEST_BASELINE.md` 定义的默认发布门禁为：

1. `python -m pytest backend/tests`
2. `cd frontend && npm test`

补充说明：

- Playwright E2E 存在，但不是默认强制门禁。
- 是否执行 E2E 取决于 AI key 环境。

---

## 6. 当前边界与已知限制

1. Chat 意图分流仍是规则驱动，复杂中文表达仍可能误判。
2. 推荐安全拦截仍是关键词级，不是结构化配料识别。
3. Chat 和 `/estimate` 的成功结果都不会自动写入 Food Log。
4. 独立 `/estimate` API 已实现，但没有正式主 UI 入口。
5. Insights 只保留同一时间范围下的最新快照，不保留多版本并存历史。
6. Food Log 已有基础检索与排序，但还不是完整的可组合高级筛选系统。
7. 图片能力当前已具备字段、治理与展示基础，但“标准菜品级 AI 图片生成 + 后台审核入库 + 全局复用”的闭环尚未进入主保存流程。
8. 当前项目已有首期中文食物知识库与检索增强，但仍属于种子库阶段，离“覆盖更全、标准化更强、持续运营的数据底座”还有差距。

---

## 7. 当前版本对外描述建议

建议对内对外统一描述为：

> Food Pilot 当前是一款以聊天为主入口的营养助手。用户登录后可在 Chat 中获取餐食推荐或营养估算，把值得保留的结果显式保存到 Food Log，并在 Insights 中做日/周分析。Profile 用于个性化与过敏约束。当前核心闭环已成立，已具备首期中文食物知识增强；下一阶段重点优化方向集中在更贴近中国用户的标准化食物数据、标准菜品视觉资产库建设、Food Log 自动补图能力、意图分流准确率和 Insights 趋势表达。

---

## 8. 下一阶段行动计划

任务 1（已完成）：提升 Chat 意图分流准确率

结论（2026-03-20）：

- 已复盘并优化推荐、估算、普通文本、澄清提问之间的判定逻辑。
- 已补充典型误判样本回归测试，优先覆盖比较句、替代问法、解释型追问和估算问法变体。
- 在保持消息契约与主流程兼容的前提下，修正了已知高频误判。

任务 2（已完成）：明确独立 `/estimate` API 的产品定位

结论（2026-03-20）：

- `/estimate` 定位为系统能力，不是当前版本用户主入口。
- Chat 作为统一用户入口；`/estimate` 负责原子化估算能力。
- 当前版本不新增独立 Estimate 主入口页面；保留 API 与前端 client 作为内部能力。
- 后续如扩展 Quick Add、批量估算、第三方复用，基于该能力层演进。

任务 3（已完成）：增强 Insights 周分析的趋势表达与可解释性

结论（2026-03-20）：

- 已在不改动 `/api/insights/analyze` 接口与主数据结构前提下，增强周分析的趋势表达，新增周趋势、波动、周期特征与一周热量轨迹展示。
- 已复用既有聚合结果、历史快照优先与重新分析流程，保持空状态、历史命中、重新分析等核心路径行为一致。
- 已补充周分析展示逻辑与模式状态切换相关前端测试，并补充后端周模式提示词趋势上下文测试。
- 周模式相较日模式具备明确区分：日模式强调单日摄入差距，周模式强调趋势变化与工作日/周末节律。

任务 4（已完成）：继续补强 Food Log 的检索与筛选能力

结论（2026-03-21）：

- 已将 Food Log 检索能力从“基础列表浏览”推进为“以搜索与筛选为中心”的使用路径，保留既有页面结构与交互心智。
- 已稳定支持关键词检索、排序切换与时间范围筛选的组合使用，能够覆盖高频历史条目定位场景。
- 已在轻量交互前提下补强筛选效率，避免引入重型筛选面板，保持上手成本可控。
- 已将筛选能力定位为“可复用结果库”导向，弱化餐次维度在检索流程中的优先级。
- Food Log 的保存、编辑、恢复与来源回跳主流程保持兼容，未引入行为回归。
- 当前能力已满足阶段目标；后续扩展方向聚焦数值范围与来源聚合筛选等增强项。

任务 5（已完成）：为 Insights 增加更明确的结果时效提示

结论（2026-03-23）：

- 已梳理并明确 Insights 历史结果、重新分析与 Food Log 变更之间的关系，确认当前结果为快照语义。
- 已增强“数据可能已变化，请重新分析”的时效提示，降低将快照误解为实时结果的风险。
- 在不改变快照模型与数据来源逻辑前提下，完成时效性反馈优化与用户预期管理。
- 历史结果优先加载、空状态展示与重新分析流程保持一致，未引入主链路行为回归。
- 已补充相关前端状态与展示测试，覆盖提示展示与流程一致性。

任务 6（已完成）：规范图片来源治理与审计字段

结论（2026-03-23）：

- 已将图片治理规则收敛到 Food Log service 层，统一处理 `image / imageSource / imageLicense` 的默认补齐与校验，避免入口分叉。
- 已明确字段职责：`image` 条件必填，`imageSource` 与 `imageLicense` 为审计字段并在有图时强制落库（支持默认补齐）。
- 已引入来源与授权的规范化枚举及别名映射，确保关键图片元数据保存行为稳定一致。
- 保持现有 `food_logs` 数据结构不变，未引入额外迁移，主链路兼容。
- 已补充 API 与 service 级测试，覆盖默认补齐、非法输入拦截、别名规范化与清空图片时的元数据联动清理。

任务 7：引入中文食物知识库 / RAG（拆分）

任务 7-A（已完成并收尾）：首期中文知识检索增强落地

完成总结：

- 已落地离线种子库：`backend/data/chinese_food_kb_seed.json`，覆盖主食、蛋白、菜品、饮品、零食等高频中文食物。
- 已实现检索服务：`backend/services/food_knowledge.py`，支持中文识别、别名匹配、打分排序、Top-K 检索与上下文拼装。
- 已接入三条主链路：`estimate / meal_recommendation / text`，并在结果中透传 `knowledge_refs`。
- 已保证主链路稳定性：增强逻辑可开关、可降级，未改变既有消息路由、保存规则与接口契约。
- 已建立验证闭环：单测、集成测试、提示词上下文测试、脚本校验均已覆盖。

收尾结论（2026-03-23）：

- 任务 7-A 已结项，不再作为“未完成项”跟踪。
- 当前交付定位为“首期可用版”，能力边界明确：本地种子库 + 规则检索，不引入向量库和在线抓取。

任务 7-B（已完成）：扩充中文食物知识覆盖（数据层）

结论（2026-03-24）：

- 已完成中文食物知识库扩容，主数据文件 `backend/data/chinese_food_kb_seed.json` 当前共 `67` 条条目，覆盖主食、地方面粉、外卖常见套餐、早餐小吃、品牌化饮品变体等高频与次高频中文食物。
- 已补齐长尾别名与地区化叫法，新增覆盖包括 `兰州拉面 / 热干面 / 螺蛳粉 / 黄焖鸡米饭 / 鱼香肉丝 / 酸菜鱼 / 肉夹馍 / 煎饼果子 / 生椰拿铁 / 低糖珍珠奶茶 / 杨枝甘露` 等典型查询表达。
- 已将结构化字段一致性纳入发布门禁：`canonical_name / aliases / nutrition_per_100g / source_ids / updated_at` 必填并做格式校验，新增条目关键字段缺失率为 `0`。
- 已强化来源可追溯链路，所有命中引用均稳定输出 `food_name + source_id + source_name`，并校验 `source_ids` 必须映射到已声明来源。
- 已完成数据版本治理，本次增量将知识库 `version` 提升至 `2026-03-24`，并在主数据中新增 `change_summary` 记录版本摘要与新增条目数量。

任务 7-C（已完成）：提升检索精度与评估可观测性（算法与质量层）

结论：

- 已将检索排序从单层规则累加升级为多特征打分，补充了同义词归一、组合菜名覆盖、别名优先、修饰词对齐、对比问法与场景加权，提升了组合菜名、近义表达与别名问法下的排序稳定性。
- 已建立固定回归评估集，覆盖 `estimate / meal_recommendation / text` 三条链路下的估算、推荐、对比、替换、解释性追问与负样本无命中场景。
- 已引入质量监控指标与仓库内基线：命中率、Top-1 准确率、Top-K 召回率、引用完整率、无命中率、负样本通过率，并在脚本中对比 `legacy / enhanced` 两套评分结果。
- 固定评估集当前基线结果为：`legacy` Top-1 准确率 `77.4%`、Top-K 召回率 `90.0%`；`enhanced` Top-1 准确率 `100%`、Top-K 召回率 `100%`，相对基线提升分别约 `29.2%` 和 `11.1%`。
- 命中样本引用完整率保持 `100%`，无命中率未回升，负样本无命中通过率保持 `100%`。
- 失败降级与开关策略保持不变；数据集不可用、空查询、非中文查询、无命中等分支仍按原有原因码返回，不影响主链路可用性。
- 新增评估脚本、固定评估集、质量基线与回归测试已纳入发布门禁；`scripts/run_release_gate.ps1` 可在本地一键执行后端测试、检索质量验证与前端测试。
- `estimate / recommendation / text` 三条链路回归测试已全部通过，未出现契约字段回退。

任务 8（已完成）：扩展更长周期的趋势分析能力

结论：
- 本任务按首期范围收敛后，已视为完成。当前 Insights 虽仍以日/周双模式为主，但周模式已经承载阶段性趋势复盘能力，满足“在约定周期内查看核心饮食趋势变化”的目标。
- 现有实现基于底层聚合数据生成趋势结果，而非仅输出泛化文案。周模式在聚合总热量、蛋白质、碳水、脂肪等基础指标之上，补充了按天序列、前后半周变化、波动幅度、工作日/周末节律等结构化趋势信息。
- 前端已提供周趋势视图与多指标切换，能够展示热量轨迹、宏量营养素序列、峰值/低值、波动等级和周期特征；后端周分析提示词也会注入对应趋势上下文，保证 AI 反馈与底层汇总一致、可解释。
- 现有日/周分析主链路保持兼容，历史快照、缓存键和分析结果存储机制未被破坏；周趋势增强是在原有 Insights 闭环上增量完成的。
- 趋势计算与展示相关验证已补齐，周模式提示词测试、历史快照测试、前端趋势计算测试和 Insights UI 状态测试均已覆盖并通过。
- 后续若扩展到 30 天或更长周期，可视为本任务后的进一步增强，而不影响当前任务已完成的结论。

任务 9（已完成）：建设更贴近中国用户的真实食物数据底座

结论（2026-03-24）：

- 已在现有中文知识库基础上，进一步收敛为“标准化食物目录”方向，而不是继续堆积零散条目。
- 已将稳定标识提升为 `food_id` 语义，并补充 `scene_tags / region_tags` 等结构化扩展位，为后续持续扩容和标准菜品视觉资产绑定提供统一主键基础。
- 已扩充中国高频场景覆盖，新增便利店、轻食、健身餐、早餐等条目，使知识库条目数提升至 `73` 条。
- 已把 `food_id / category / portion_hints / source_ids / updated_at` 等关键结构纳入数据门禁，并为可选的组合菜拆分与场景标签增加校验规则，确保后续维护可持续。
- 已同步扩充固定评估集并通过检索质量验证，当前 `enhanced` 检索基线仍保持 `Top-1 = 100%`、`Top-K = 100%`。

当前边界：

- 当前交付仍是仓库内版本化离线数据集，不是独立运营数据库。
- 当前阶段仍不承诺一次性覆盖完整全国菜品，也不引入高成本在线抓取与实时外部同步。

任务 10：建设标准菜品视觉资产库与 Food Log 自动补图闭环

拆分原则：

- 该任务不建议一次性端到端硬落地，应拆为“标准菜品图片资产基础能力”“生成触发与生命周期编排”“Food Log 复用接入”三个连续子任务。
- Admin 审核台虽然服务于同一目标，但其访问控制、运营流程和页面信息架构独立，应单列为下一任务，不与普通用户侧能力混做。

目标：

- 将菜品图片能力从“单条 Food Log 可选图片字段”升级为“标准菜品级官方视觉资产”。
- 通过统一风格的 AI 生成、后台审核与审核后全局复用，为常见标准菜品提供稳定、统一的产品图片。

任务 10-A（已完成）：标准菜品图片资产基础能力

结论（2026-03-25）：

- 已在数据库初始化层补充 `standard_dishes` 与 `dish_images` 两张表，为标准菜品图片资产提供持久化基础，并新增索引、约束与更新时间触发器。
- 已在 `standard_dishes` 上落地标准菜品级视觉字段：`image_url`、`image_status`、`image_prompt_version`、`image_updated_at`，可稳定表达“无图 / 待审核 / 已通过 / 已拒绝”状态。
- 已通过 `dish_images` 承载候选图历史与审核生命周期，状态流明确为 `null -> pending -> approved / rejected`；其中 `approved` 候选会同步回写为 `standard_dish` 当前唯一官方图。
- 已在数据层保证“同一标准菜品仅有一个 pending 候选、仅有一个 approved 官方图”，避免并发或重复审核导致用户侧默认图冲突。
- 已补充标准菜品图片资产 repository / service，支持标准菜品创建、候选图写入、审核通过、审核拒绝、候选列表读取以及“是否可触发生成”的统一判断。
- 已补充回归测试，覆盖建表结果、名称归一、待生成状态识别、唯一官方图约束、拒绝后恢复可触发状态，以及“候选图被拒绝不影响既有 Food Log 保存链路”。

当前边界：

- 本子任务完成的是“标准菜品图片资产基础能力”，尚未包含 AI 生成任务编排、幂等生成调度与 Food Log 用户侧图片复用接入。
- `dish_images` 当前已具备审核状态与审计字段，但管理员审核台与重生成操作仍归属于后续任务 10-B / 任务 11。

任务 10-B（已完成能力层）：AI 生成触发与生命周期编排

结论（2026-03-25）：

- 已在数据库初始化层补充 `image_generation_jobs` 表，为标准菜品图片生成任务提供独立持久化载体，并新增状态索引、唯一活跃任务索引与更新时间触发器。
- 已在任务层明确 `queued -> running -> completed / failed / timed_out` 生命周期，同时记录 `prompt_version`、`prompt_text`、`dish_image_id`、`retry_of_job_id`、错误信息与开始/结束时间，满足后续排查与审计。
- 已在数据层保证“同一标准菜品同一时刻仅允许一个活跃生成任务”，并将标准菜品的“是否可触发生成”判断收敛为统一规则，避免高并发场景下重复提交多个任务。
- 已固化统一 Prompt Template，并将 prompt 版本、模型、超时、mock 图地址等能力收敛到配置层，避免后续多入口硬编码 prompt。
- 已补充标准菜品图片生成编排 service，支持幂等入队、后台异步执行、进程启动后的任务恢复与重新派发，以及失败 / 超时任务的显式重试。
- 已将生成结果约束为先写入 `dish_images` 候选记录并进入 `pending`，不会直接回写为用户可见正式图；只有审核通过后才允许成为标准菜品官方图。
- 已补充回归测试，覆盖建表结果、单菜品唯一活跃任务、成功生成候选图、失败与超时审计、失败后重试，以及进程重启后的运行中任务回收与待执行任务恢复。

当前边界：

- 本子任务完成的是“生成编排能力层”；业务侧自动触发入口与用户侧官方图复用已在后续任务 10-C 中完成闭环。
- 管理员审核台与人工 `regenerate` 操作入口仍归属于任务 11；当前已具备底层任务重试能力，但尚未开放后台页面与接口闭环。
- 当前实现已具备统一 Prompt Template 与 `dish_name` 注入能力，但尚未完成“仅输入菜名 -> 系统内部自动扩写为更具体图片 prompt”的能力；现阶段仍主要依赖通用模板。

任务 10-C（已完成）：Food Log 图片复用与用户侧回退逻辑

结论（2026-03-26）：

- 已在 Food Log 保存链路接入标准菜品绑定：当结果高置信命中标准菜品且名称可稳定映射到知识库标准名时，后端会写入 `standard_dish_id`，并以非阻塞方式补触发标准菜品图片生成任务。
- 已在 Food Log 列表、详情与单条读取返回中接入标准菜品官方图复用；当 `standard_dish.image_status = approved` 且当前 Food Log 自身无图时，用户侧稳定展示标准菜品官方图。
- 已明确用户侧回退逻辑：若未命中标准菜品、命中置信度不足，或图片仍处于 `null / pending / rejected`，则继续展示占位图，不暴露候选图状态与审核中图片。
- 已补强 Chat 侧模糊标准菜品澄清规则，类似“炒面热量”这类泛化表达会先追问，不直接产出可保存卡片，也不沉淀模糊 `standard_dish_id`。
- 图片增强层保持非阻塞，不改变 Food Log 的保存成功率、估算结果、编辑能力与既有主流程。

当前边界：

- 当前完成的是普通用户侧闭环：标准菜品绑定、官方图复用与回退逻辑已打通。
- 管理员审核台、人工 `approve / reject / regenerate` 操作与权限控制仍归属于任务 11。

任务 10 的总体约束：

- 图片资产绑定在 `standard_dish`，不绑定在单条 `food_log`。
- 所有图片必须通过统一 Prompt Template 生成，保证构图、角度、背景、光线与风格一致。
- AI 结果先进入 `pending` 审核状态；审核通过前，用户侧继续显示占位图，不直接暴露候选图。
- Food Log 保存链路只负责标准菜品匹配与图片复用判断，不因图片生成或审核状态阻塞主保存流程。

任务 11（已完成）：建设 Admin 菜品图片审核台（仅管理员可见）

结论（2026-03-30）：

- 已落地独立 Admin 审核入口：后端提供 `/admin/dish-images` 列表、详情、`approve`、`reject`、`regenerate` 接口，前端提供 `AdminDishImageReviewPage` 审核台页面，能够完成候选图浏览、筛选、详情查看与审核动作闭环。
- 已完成管理员权限收口：后端通过 `require_admin_user` 统一拦截非管理员访问；前端仅对 `isAdmin = true` 用户暴露入口，普通用户不会在主工作区导航中看到该后台能力，也无法调用对应审核接口。
- 已完成审核动作与官方图同步：`approve` 会将候选图写回 `standard_dish` 官方图；`reject` 会保留候选图为不可见状态；`regenerate` 会重新进入生成流程且不清空当前已批准官方图，符合既定产品约束。
- 已补齐审计链路：`dish_images` 已记录审核人、审核时间与审核备注，`dish_image_admin_events` 会持久化 `approve / reject / regenerate` 操作事件；后台详情页可回看最近操作记录，满足排查与审计需求。
- 已验证普通用户隔离与管理员闭环：相关后端测试覆盖非管理员访问拦截、列表筛选、审核通过、重生成与官方图保持逻辑；本次复查使用仓库虚拟环境执行 `backend/tests/test_admin_dish_image_api.py`、`backend/tests/test_standard_dish_repository.py`、`backend/tests/test_standard_dish_image_generation_service.py`，共 `18` 项全部通过。

当前边界：

- 该能力仍是内部 Admin 工具，不进入普通用户主导航与公开产品功能描述。
- 不做开放式网络搜图，不抓取第三方图片入库，不开放用户上传作为标准菜品图片来源，也不把图片能力扩展为识别入口。

后续建议任务 12：补齐标准菜品图片 Prompt 自动扩写层

目标：

- 将标准菜品图片生成从“统一模板 + 菜名占位”升级为“统一模板 + 菜型感知扩写 + 通用回退”的稳定出图链路。
- 保持用户侧输入仍然只有菜名或标准菜名，不增加额外 prompt 编写负担。

范围：

- 在后端图片生成服务中新增内部 prompt builder，根据标准菜品名输出结构化扩写结果或扩写标签。
- 先覆盖高频菜型，如 `面 / 粉 / 盖饭 / 炒饭 / 炒面 / 披萨 / 汉堡 / 沙拉 / 汤 / 甜品 / 饮品`。
- 为无法稳定识别菜型的条目保留通用模板回退，确保主流程不因扩写失败而中断。
- 为扩写逻辑补充可回归测试，至少覆盖高频菜型命中、模糊菜名回退与 prompt 版本升级审计。

---

## 9. 结论

截至 2026-03-24，Food Pilot 已经不是纯 demo。

它已经具备：

- 真实账号体系
- 真实 Chat 会话与消息持久化
- 显式保存的 Food Log
- 可用的 Profile 个性化能力
- 历史快照优先的 Insights 分析闭环
- 首期面向中国用户的标准化食物数据底座

当前最需要继续收敛的不是“再加多少页面”，而是：

- Chat 规则分流的稳定性
- Insights 的分析表达与状态模型
- 独立 estimate 路径与 Chat 统一链路的工程收敛
- 标准菜品级 AI 图片生成、审核入库与 Food Log 自动补图闭环
